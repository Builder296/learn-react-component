import React from "react";

const MenuBar = () => {

    return <div className="py-3 h-12 sticky top-0 max-w-8xl mx-auto bg-white flex-none flex">
        <p>Menu bar & Always hold on top page</p>
    </div>
}

export default MenuBar;